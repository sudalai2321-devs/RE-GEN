'use client'; import { useMemo, useState } from 'react'; import Link from 'next/link'; import { Filter, Search, Target } from 'lucide-react';
const rows=[['Distributed cold-chain anomaly detection','Cold Chain','Logistics operators need earlier signals for abnormal equipment behavior.','Moderate'],['Sparse-connectivity wildlife monitoring','Conservation','Field systems need low-power filtering before transmitting events.','Strong'],['Smallholder irrigation leak localization','Water Systems','Operators need localized signals before water loss compounds.','Moderate'],['Remote infrastructure inspection','Infrastructure','Inspection workflows can be constrained by limited site access.','Strong']];
export default function Problems(){const [q,setQ]=useState('');const filtered=useMemo(()=>rows.filter(r=>r.join(' ').toLowerCase().includes(q.toLowerCase())),[q]);return <main className="content"><div className="section-head"><div><h1 className="page-title">Problem explorer</h1><p className="page-subtitle">Structured problem records intended to be evidence-backed before they are matched to capabilities.</p></div><button className="btn"><Filter size={14}/> Filters</button></div><div className="card card-pad"><div style={{position:'relative'}}><Search size={14} color="#66717d" style={{position:'absolute',left:12,top:12}}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search problems, domains, contexts…" style={{width:'100%',background:'#0a0e11',border:'1px solid var(--line)',padding:'10px 12px 10px 35px',borderRadius:9,color:'#e7ebee'}}/></div></div><div className="mt-6 grid grid-2">{filtered.map(r=><div className="card card-pad" key={r[0]}><div style={{display:'flex',justifyContent:'space-between'}}><span className="status green">{r[1]}</span><Target size={16} color="#c8ff54"/></div><h3 style={{fontSize:15,margin:'10px 0 6px'}}>{r[0]}</h3><p style={{fontSize:11,color:'#7f8993',lineHeight:1.7,margin:0}}>{r[2]}</p><div className="mt-3" style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span className="status blue">Evidence {r[3]}</span><Link href="/opportunities/o1" className="link">View problem →</Link></div></div>)}</div></main>}
