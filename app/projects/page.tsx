import Link from 'next/link'; import { ArrowRight, BriefcaseBusiness, Plus } from 'lucide-react'; import { projects } from '../../lib/data';
export default function Projects(){return <main className="content"><div className="section-head"><div><h1 className="page-title">Projects</h1><p className="page-subtitle">Documented innovation attempts and their current analysis state.</p></div><Link href="/analyze" className="btn primary"><Plus size={14}/> Add project</Link></div><div className="grid grid-2">{projects.map(p=><div className="card card-pad" key={p.id}><div style={{display:'flex',justifyContent:'space-between',alignItems:'start'}}><div><span className="status green">Project</span><h3 style={{fontSize:16,margin:'10px 0 3px'}}>{p.name}</h3><div className="stat-meta">{p.domain}</div></div><BriefcaseBusiness size={17} color="#7e8892"/></div><div className="grid grid-3 mt-4"><Mini label="Evidence" value={String(p.evidence)}/><Mini label="Opportunities" value={String(p.opportunities)}/><Mini label="Updated" value={p.updated}/></div><div className="mt-4" style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span className={`status ${p.status==='DNA Extracted'?'green':p.status==='Gap Detected'?'amber':'blue'}`}>{p.status}</span><Link className="btn" href={`/projects/${p.id}`}>Open <ArrowRight size={13}/></Link></div></div>)}</div></main>}
function Mini({label,value}:{label:string,value:string}){return <div style={{borderTop:'1px solid var(--line)',paddingTop:9}}><div className="stat-kicker">{label}</div><div style={{fontSize:12,fontWeight:700,marginTop:5}}>{value}</div></div>}
