import Link from 'next/link'; import { ArrowRight, GitBranch, ShieldCheck } from 'lucide-react'; import { dna } from '../../../../lib/data';
export default function DNA(){return <main className="content"><div className="section-head"><div><span className="status green">DNA Extracted</span><h1 className="page-title" style={{marginTop:10}}>Innovation DNA</h1><p className="page-subtitle">Structured project understanding with confidence, status and evidence references.</p></div><Link href="/projects/p1/gap" className="btn primary">Detect Gap <ArrowRight size={14}/></Link></div><div className="card card-pad"><div className="section-head"><div><div className="section-title">DNA relationship map</div><div className="section-note">Click through to inspect supporting records</div></div><GitBranch size={16} color="#c8ff54"/></div><div className="callout"><strong>Traceable by design.</strong> Every item below carries evidence count and confidence. Unsupported information should be returned as UNKNOWN rather than silently inferred.</div><div className="dna-grid mt-4">{dna.map(x=><div className="dna-item" key={x.cat}><div className="dna-title">{x.cat}</div><div className="dna-value">{x.value}</div><div className="dna-foot"><Link href="/evidence" className="evidence-mini">View evidence →</Link><span className={`status ${x.confidence==='High'?'green':'amber'}`}>{x.confidence}</span></div></div>)}</div></div><div className="mt-6 card card-pad"><div className="section-head"><div className="section-title">Evidence states</div><ShieldCheck size={16} color="#c8ff54"/></div><div className="grid grid-3">{[['Supported','5+ linked records','green'],['Partially supported','2 need corroboration','amber'],['Unknown / uncertain','1 flagged for review','blue']].map(([a,b,k])=><div className="quick-link" key={a}><span className={`status ${k}`}>{a}</span><span>{b}</span></div>)}</div></div></main>}
