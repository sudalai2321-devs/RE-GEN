import Link from 'next/link'; import { ArrowRight, Filter } from 'lucide-react'; import { opportunities } from '../../../../lib/data';
export default function ProjectOpp(){return <main className="content"><div className="section-head"><div><h1 className="page-title">Cross-domain opportunities</h1><p className="page-subtitle">Candidate matches retrieved from project capabilities, gap context and documented problem records.</p></div><button className="btn"><Filter size={14}/> Filters</button></div><div className="space-y">{opportunities.map(o=><div className="card card-pad" key={o.id}><div style={{display:'grid',gridTemplateColumns:'1fr auto',gap:18}}><div><div style={{fontSize:10,color:'#606a75',textTransform:'uppercase',letterSpacing:'.12em'}}>{o.source} → {o.targetDomain}</div><h3 style={{fontSize:16,margin:'8px 0'}}>{o.target}</h3><p style={{margin:0,color:'#7b8590',fontSize:11,lineHeight:1.7}}>{o.capability} is the primary transferable capability identified for this candidate. Main limitation: {o.limitation}.</p></div><div style={{textAlign:'right'}}><span className="status green">{o.status}</span><div style={{marginTop:9,color:'#7d8791',fontSize:10}}>{o.evidence} evidence records</div></div></div><div className="mt-4" style={{display:'flex',justifyContent:'flex-end'}}><Link className="btn" href={`/opportunities/${o.id}`}>Open intelligence report <ArrowRight size={13}/></Link></div></div>)}</div></main>}
