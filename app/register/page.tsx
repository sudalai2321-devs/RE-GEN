'use client'; import { useState } from 'react'; import Link from 'next/link'; import { UserPlus } from 'lucide-react';
export default function Register(){const [created,setCreated]=useState(false);return <main className="content" style={{maxWidth:760}}><div className="card card-pad"><div style={{display:'flex',justifyContent:'space-between'}}><div><span className="status green">New workspace</span><h1 className="page-title" style={{fontSize:30,marginTop:10}}>Create account</h1><p className="page-subtitle">Set up a workspace for evidence-grounded innovation analysis.</p></div><UserPlus size={22} color="#c8ff54"/></div><div className="form-grid mt-5"><div className="form-field"><label>Name</label><input placeholder="Your name"/></div><div className="form-field"><label>Email</label><input type="email" placeholder="you@example.com"/></div><div className="form-field"><label>Password</label><input type="password" placeholder="Create password"/></div><div className="form-field"><label>Workspace</label><input placeholder="Innovation Lab"/></div></div><button className="btn primary mt-4" onClick={()=>setCreated(true)}>Create account</button>{created&&<div className="callout mt-4"><strong>Workspace created in demo UI.</strong> Connect the form to the backend authentication service to persist users and roles.</div>}<div className="stat-meta mt-4">Already registered? <Link href="/login" className="link">Sign in →</Link></div></div></main>}
