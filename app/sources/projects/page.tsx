'use client'; import { useState } from 'react'; import { Database, Search, Upload } from 'lucide-react';
const results=[['Predictive maintenance pilot — university repository','Academic','Industrial IoT','Source record available'],['Cold-chain reliability initiative','Government / public','Cold Chain','Curated catalog record'],['Remote anomaly monitoring challenge','Innovation challenge','Infrastructure','Curated catalog record']];
export default function SourceProjects(){const [q,setQ]=useState('');return <main className="content"><div className="section-head"><div><h1 className="page-title">Find documented projects</h1><p className="page-subtitle">Search the curated source catalog instead of silently scraping the web.</p></div><button className="btn"><Upload size={14}/> Import URL</button></div><div className="card card-pad"><div style={{position:'relative'}}><Search size={14} style={{position:'absolute',left:12,top:12}}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search documented projects…" style={{width:'100%',background:'#0a0e11',border:'1px solid var(--line)',borderRadius:9,padding:'10px 12px 10px 35px',color:'#eef1f4'}}/></div></div><div className="grid grid-2 mt-6">{results.filter(r=>r.join(' ').toLowerCase().includes(q.toLowerCase())).map(r=><div className="card card-pad" key={r[0]}><span className="status green">{r[1]}</span><h3 style={{fontSize:14,margin:'9px 0 4px'}}>{r[0]}</h3><div className="stat-meta">{r[2]} · {r[3]}</div><div className="mt-4"><button className="btn primary"><Database size={13}/> Import Project</button></div></div>)}</div></main>}
