'use client'; import { useState } from 'react'; import { Check, ExternalLink, FileSearch, X } from 'lucide-react'; import { evidence } from '../../lib/data';
export default function Evidence(){const [selected,setSelected]=useState<typeof evidence[number]|null>(null); return <main className="content"><div className="section-head"><div><h1 className="page-title">Evidence review</h1><p className="page-subtitle">Review claim → evidence → source relationships. Conflicting records remain separate.</p></div><span className="status amber">17 in queue</span></div><div className="space-y">{evidence.map(ev=><div className="card card-pad" key={ev.id}><div style={{display:'flex',justifyContent:'space-between',gap:15}}><div><span className="mono" style={{color:'#63707c',fontSize:9}}>{ev.id}</span><h3 style={{fontSize:14,margin:'7px 0 4px'}}>{ev.claim}</h3><div style={{fontSize:10,color:'#6f7984'}}>{ev.source} · {ev.publisher}</div></div><span className={`status ${ev.status==='Verified'?'green':'amber'}`}>{ev.status}</span></div><div className="mt-3" style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div className="excerpt" style={{flex:1}}>{ev.excerpt}</div><div style={{display:'flex',gap:8,marginLeft:10}}><button className="btn" onClick={()=>setSelected(ev)}>View Evidence</button><button className="btn"><Check size={13}/> Verify</button></div></div></div>)}</div>{selected && <div className="drawer-backdrop" onClick={()=>setSelected(null)}><div className="drawer" onClick={e=>e.stopPropagation()}><div className="drawer-head"><div><div className="mono" style={{fontSize:9,color:'#66717c'}}>{selected.id}</div><h3>{selected.claim}</h3></div><button className="icon-btn" onClick={()=>setSelected(null)}><X size={15}/></button></div><div className="space-y"><div><div className="stat-kicker">Source</div><div className="mt-2">{selected.source}</div></div><div><div className="stat-kicker">Publisher</div><div className="mt-2">{selected.publisher}</div></div><div><div className="stat-kicker">Supporting excerpt</div><div className="excerpt mt-2">{selected.excerpt}</div></div><div className="grid grid-2"><button className="btn primary"><Check size={13}/> Mark Verified</button><button className="btn"><FileSearch size={13}/> Needs Review</button></div><button className="btn"><ExternalLink size={13}/> Open original source</button></div></div></div>}</main>}
