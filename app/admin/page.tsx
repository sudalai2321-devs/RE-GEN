import Link from 'next/link'; import { Database, FileSearch, FlaskConical, Settings2, ShieldCheck } from 'lucide-react';
const items=[['Sources','Manage, verify, deactivate and reprocess source records.','/sources',Database],['Projects','Inspect project records and re-run analysis.','/projects',Settings2],['Evidence','Review unsupported and conflicting claims.','/evidence',ShieldCheck],['Jobs','View processing state and retry failures.','/experiments',FlaskConical],['Analysis Runs','Inspect model, prompt version, status and error history.','/dashboard',FileSearch]];
export default function Admin(){return <main className="content"><div className="section-head"><div><span className="status red">ADMIN</span><h1 className="page-title" style={{marginTop:10}}>System control plane</h1><p className="page-subtitle">Frontend shell for source, project, evidence, job and analysis-run administration.</p></div></div><div className="grid grid-2">{items.map(([title,desc,href,Icon])=>{const C=Icon as React.ElementType;return <Link href={href as string} className="card card-pad" key={title as string}><div style={{display:'flex',justifyContent:'space-between'}}><span className="status green">Control</span><C size={17} color="#c8ff54"/></div><h3 style={{fontSize:15,margin:'10px 0 4px'}}>{title as string}</h3><p style={{margin:0,color:'#7c8690',fontSize:11,lineHeight:1.65}}>{desc as string}</p></Link>})}</div></main>}
