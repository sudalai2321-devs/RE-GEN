'use client'; import { useState } from 'react'; import Link from 'next/link'; import { ArrowRight, LockKeyhole } from 'lucide-react';
export default function Login(){const [submitted,setSubmitted]=useState(false);return <main className="content" style={{maxWidth:700}}><div className="card card-pad"><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><div><span className="status green">Secure access</span><h1 className="page-title" style={{fontSize:30,marginTop:10}}>Sign in</h1><p className="page-subtitle">Access your private projects, evidence reviews and experiments.</p></div><LockKeyhole size={22} color="#c8ff54"/></div><div className="form-grid mt-5"><div className="form-field full"><label>Email</label><input type="email" placeholder="you@example.com"/></div><div className="form-field full"><label>Password</label><input type="password" placeholder="••••••••"/></div></div><button className="btn primary" style={{marginTop:16}} onClick={()=>setSubmitted(true)}>Sign in <ArrowRight size={14}/></button>{submitted&&<div className="callout mt-4"><strong>Demo login accepted.</strong> Authentication is represented in the frontend prototype; connect this form to the server-side auth layer before production.</div>}<div className="stat-meta mt-4">No account? <Link href="/register" className="link">Create one →</Link></div></div></main>}
