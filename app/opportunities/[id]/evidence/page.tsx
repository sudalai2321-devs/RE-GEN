import Link from 'next/link'; import { ArrowLeft, ExternalLink, FileSearch } from 'lucide-react'; import { evidence } from '../../../../lib/data';
export default function OppEvidence(){return <main className="content"><div className="section-head"><div><Link className="btn subtle" href="/opportunities/o1"><ArrowLeft size={13}/> Back</Link><h1 className="page-title" style={{marginTop:14}}>Opportunity evidence map</h1><p className="page-subtitle">Claim → Evidence → Source. Relevant excerpts only; source records remain traceable.</p></div></div><div className="space-y">{evidence.map(ev=><div className="card card-pad" key={ev.id}><div style={{display:'flex',justifyContent:'space-between',gap:14}}><div><span className="mono" style={{fontSize:9,color:'#63707c'}}>{ev.id}</span><h3 style={{fontSize:14,margin:'7px 0'}}>{ev.claim}</h3></div><span className={`status ${ev.status==='Verified'?'green':'amber'}`}>{ev.status}</span></div><div className="grid grid-2 mt-3"><div><div className="stat-kicker">Source</div><div style={{marginTop:6,fontSize:11}}>{ev.source} · {ev.publisher}</div></div><div><div className="stat-kicker">Confidence / date</div><div style={{marginTop:6,fontSize:11}}>{ev.confidence} · {ev.date}</div></div></div><div className="excerpt mt-3">{ev.excerpt}</div><div className="mt-3" style={{display:'flex',gap:8}}><Link href="/evidence" className="btn"><FileSearch size={13}/> Review record</Link><button className="btn"><ExternalLink size={13}/> Open original</button></div></div>)}</div></main>}
