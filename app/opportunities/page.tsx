import Link from 'next/link'; import { ArrowRight, Filter, Lightbulb } from 'lucide-react'; import { opportunities } from '../../lib/data';
export default function Opportunities(){return <main className="content"><div className="section-head"><div><h1 className="page-title">Opportunity library</h1><p className="page-subtitle">Explore candidate transfer paths without hiding the evidence and uncertainties behind a single score.</p></div><button className="btn"><Filter size={14}/> Filter & sort</button></div><div className="grid grid-2">{opportunities.map(o=><div className="card card-pad" key={o.id}><div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}><span className="status green">{o.status}</span><Lightbulb size={16} color="#c8ff54"/></div><h3 style={{fontSize:16,margin:'11px 0 6px'}}>{o.target}</h3><div style={{color:'#6f7984',fontSize:10}}>{o.source} → {o.targetDomain}</div><div className="callout mt-3"><strong>Transferable capability:</strong> {o.capability}</div><div className="grid grid-2 mt-3"><Mini label="Evidence" value={`${o.evidence} records`}/><Mini label="Main limitation" value={o.limitation}/></div><div className="mt-4" style={{display:'flex',justifyContent:'flex-end'}}><Link href={`/opportunities/${o.id}`} className="btn">View opportunity <ArrowRight size={13}/></Link></div></div>)}</div></main>}
function Mini({label,value}:{label:string,value:string}){return <div><div className="stat-kicker">{label}</div><div style={{fontSize:11,lineHeight:1.6,marginTop:5,color:'#c4cad0'}}>{value}</div></div>}
